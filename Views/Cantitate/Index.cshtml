@model IEnumerable<JueriOnlineShop.Models.Cantitate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cantitatateTotala)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cantitatateTotala)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idCantitate">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idCantitate">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idCantitate">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
